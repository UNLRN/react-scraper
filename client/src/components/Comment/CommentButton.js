import React from 'react';

const CommentButton = (props) => {
    return (
        <div>
            <button type="button" class="btn btn-default" onClick={this.props.eventHandler}>Comment</button>
        </div>
    );
};

export default CommentButton;